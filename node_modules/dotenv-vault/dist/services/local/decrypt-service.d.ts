import { DotenvConfigOutput } from 'dotenv';
interface LocalDecryptServiceAttrs {
    cmd: any;
    dotenvKey: any;
}
type InstructionsType = {
    ciphertext: string;
    key: string;
};
declare class LocalDecryptService {
    cmd: any;
    dotenvKey: any;
    log: any;
    constructor(attrs?: LocalDecryptServiceAttrs);
    run(): Promise<void>;
    _instructions(result: DotenvConfigOutput, dotenvKey: string): InstructionsType;
    get vaultPath(): string;
}
export { LocalDecryptService };
