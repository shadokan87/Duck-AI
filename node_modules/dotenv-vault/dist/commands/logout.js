"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const core_1 = require("@oclif/core");
const logout_service_1 = require("../services/logout-service");
class Logout extends core_1.Command {
    async run() {
        const { flags } = await this.parse(Logout);
        const yes = flags.yes;
        await new logout_service_1.LogoutService({ cmd: this, yes: yes }).run();
    }
}
exports.default = Logout;
Logout.description = 'Log out';
Logout.examples = [
    '<%= config.bin %> <%= command.id %>',
];
Logout.flags = {
    yes: core_1.Flags.boolean({
        char: 'y',
        description: 'Automatic yes to prompts. Assume yes to all prompts and run non-interactively.',
        hidden: false,
        required: false,
        default: false,
    }),
};
